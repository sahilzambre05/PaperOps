import { Trash2 } from 'lucide-react';
export default function HistoryTable({ items, onClear }) { return <section className="panel history-table" id="history"><div className="section-title"><div><p>RECENT ACTIVITY</p><h2>Transition history</h2></div>{items.length>0&&<button className="text-button" onClick={onClear}><Trash2 size={15}/> Clear</button>}</div>{items.length===0?<p className="empty">Predictions you generate will appear here.</p>:<div className="rows"><div className="row labels"><span>Time</span><span>Grade change</span><span>Duration</span><span>Confidence</span></div>{items.map(item=><div className="row" key={item.id}><span>{item.time}</span><span>{item.from} → {item.to} GSM</span><span>{item.duration} min</span><span>{item.confidence}%</span></div>)}</div>}</section>; }
